name: "Build-Upload-DockerImages"
on:
  workflow_dispatch:
  push:
    paths:
      - "DockerImages/**"
    branches:
      - feature
  pull_request:
    paths:
      - "DockerImages/**"
    branches:
      - develop

jobs:
  build-docker:
    name: "Build Docker Images"
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: AWS credentials
        uses: aws-actions/configure-aws-credentials@v4.0.2
        with:
          aws-access-key-id: ${{ secrets.TF_AWS_ACCESS_KEY }}
          aws-secret-access-key: ${{ secrets.TF_AWS_SECRET_KEY }}

      - name: Validate ECR connection
        id: ecr-validate
        run: |
          set -e
          if ! aws ecr describe-repositories --region ${{ secrets.AWS_REGION }} > /dev/null 2>&1; then
            echo "Error: Unable to connect to ECR. Please check your AWS credentials and region settings."
            exit 1
          fi

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2
  
      - name: Set up Docker Buildx
        uses: docker/build-push-action@v6.5.0

      # - name: Build and Push Docker Image
      #   run: for dir in ./DockerImages/*/
      #     do
      #     image_name=$(basename $dir)
      #     docker build -t my-dockerhub-user/$image_name:latest $dir
      #     docker push my-dockerhub-user/$image_name:latest
      #     done
